# pond is a local-first desktop app. Runtime secrets (ingest token) are
# stored in the macOS Keychain / Windows Credential Manager by the desktop
# app itself — there's no `.env` for the app at runtime. This file only
# exists for optional development tooling.

# Override the default port the extension talks to. Normally 41610.
# POND_INGEST_PORT="41610"
